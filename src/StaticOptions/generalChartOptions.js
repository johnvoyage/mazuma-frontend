const standardLine = {
  legend: {
    display: false,
    labels: {
      fontColor: "rgb(255, 99, 132)"
    }
  }
};

const standardPie = {};

const standardBar = {};

export default {
  standardLine,
  standardPie,
  standardBar
};
